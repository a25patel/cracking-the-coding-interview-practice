var secondPrime = require('../second_prime');

describe('#secondPrime', function(){
  xit('returns the first and second prime numbers after any given input', function(){
    var input = '';
    var expected = ;
    var actual = function(input); 

    expect(actual).toEqual(expected);
  });
});
