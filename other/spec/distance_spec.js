var distance = require('../distance.js');

describe('#distance', function(){
  xit('returns the distance between two points using the distance formula', function(){
    expect(actual).toEqual(expectedOutput);

  }),

  xit('returns the closest distance and point between players and flag', function(){
    expect(actual).toEqual(expectedOutput);
  });
});



// 1. Use the distance formula to find the distance between two things on a coordinate plane
// 2. Now have one point be a flag, the other a player, have 5 players on the coordinate, find
// the player closest to the flag.
// 3. now have a wall next to flag, find the closest player.
